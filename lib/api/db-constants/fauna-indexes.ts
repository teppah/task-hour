export enum COLLECTIONS {
  TASKS = "tasks",
  USERS = "users",
}
export enum USER_INDEXES {
  BY_EMAIL = "user_by_email",
  BY_USERID = "user_by_userId",
}

export enum TASK_INDEXES {
  BY_USERID_AND_TASKID = "task_by_userId_and_taskId",
  BY_USERID = "tasks_by_userId",
  BY_TASKID = "task_by_taskId",
}
