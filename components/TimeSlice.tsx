type Props = {};

const TimeSlice = ({}: Props) => {
  return (
    <div>
      <style jsx>{`
        div {
          @apply w-full h-8;
          @apply border border-gray-200;
        }
      `}</style>
    </div>
  );
};

export default TimeSlice;
